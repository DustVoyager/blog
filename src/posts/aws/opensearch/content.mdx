---
title: "AWS OpenSearch 완벽 가이드: 실시간 로그 분석부터 시각화까지"
desc: AWS OpenSearch Service를 활용한 대규모 로그 수집, 실시간 검색, 데이터 시각화 구축 방법을 상세히 알아봅니다
date: 2026-02-09
thumbnail: /posts/devops.png
---

![](/posts/devops.png)

# AWS OpenSearch 완벽 가이드

AWS OpenSearch Service는 Elasticsearch와 호환되는 완전 관리형 검색 및 분석 엔진입니다. 로그 분석, 실시간 애플리케이션 모니터링, 웹사이트 검색 등 다양한 사용 사례에 활용할 수 있습니다.

---

<Callout>

### 📚 AWS OpenSearch 아키텍처 가이드

프로덕션 환경을 위한 OpenSearch 클러스터 설계 및 최적화 가이드를 다운로드하세요.

<DownloadButton
  assetId="opensearch-architecture-guide"
  assetName="AWS_OpenSearch_Architecture_Guide.pdf"
  downloadUrl="/downloads/opensearch-guide.pdf"
/>

</Callout>

---

## 1. AWS OpenSearch란?

### 1.1 핵심 기능

**실시간 검색 및 분석:**
- 페타바이트 규모의 데이터에서 밀리초 단위 검색
- 실시간 로그 및 메트릭 분석
- 풀텍스트 검색, 구조화된 검색, 분석 쿼리

**데이터 시각화:**
- OpenSearch Dashboards (Kibana 호환)
- 커스텀 대시보드 및 시각화 생성
- 실시간 모니터링 및 알림

**확장성 및 가용성:**
- 자동 스케일링 지원
- 멀티 AZ 배포로 고가용성 보장
- 자동 백업 및 스냅샷

### 1.2 주요 사용 사례

1. **로그 및 이벤트 분석**: 애플리케이션 로그, 시스템 로그, 보안 이벤트 수집 및 분석
2. **실시간 모니터링**: 인프라 및 애플리케이션 성능 모니터링
3. **보안 분석**: SIEM (Security Information and Event Management)
4. **전문 검색**: 웹사이트, 앱 내 검색 기능 구현

---

## 2. OpenSearch 클러스터 아키텍처

### 2.1 클러스터 구성 요소

```
┌─────────────────────────────────────────────┐
│          Load Balancer / CloudFront          │
└─────────────────┬───────────────────────────┘
                  │
    ┌─────────────┴─────────────┐
    │                           │
┌───▼────┐                 ┌───▼────┐
│ Master │ (Availability    │ Master │
│ Node   │  Zone 1)         │ Node   │
└───┬────┘                 └───┬────┘
    │                           │
    │   ┌───────────────────┐   │
    └───►   Data Nodes      ◄───┘
        │  (Auto Scaling)   │
        └───────┬───────────┘
                │
        ┌───────▼───────────┐
        │  OpenSearch       │
        │  Dashboards       │
        └───────────────────┘
```

**Master Nodes:**
- 클러스터 상태 관리
- 인덱스 생성/삭제 조정
- 노드 추가/제거 관리

**Data Nodes:**
- 실제 데이터 저장 및 검색
- 쿼리 실행 및 집계
- 수평 확장 가능

**UltraWarm Nodes (선택 사항):**
- 비용 효율적인 웜 스토리지
- 읽기 전용 데이터에 최적화
- S3 기반 스토리지

### 2.2 인덱스 설계

```json
{
  "settings": {
    "number_of_shards": 3,
    "number_of_replicas": 2,
    "index": {
      "refresh_interval": "5s"
    }
  },
  "mappings": {
    "properties": {
      "timestamp": { "type": "date" },
      "level": { "type": "keyword" },
      "message": { "type": "text" },
      "service": { "type": "keyword" },
      "metadata": {
        "properties": {
          "user_id": { "type": "keyword" },
          "session_id": { "type": "keyword" }
        }
      }
    }
  }
}
```

---

## 3. 로그 수집 파이프라인 구축

### 3.1 Fluent Bit를 이용한 로그 수집

**Fluent Bit 설정:**

```conf
[SERVICE]
    Flush        5
    Daemon       Off
    Log_Level    info

[INPUT]
    Name              tail
    Path              /var/log/app/*.log
    Parser            json
    Tag               app.logs
    Refresh_Interval  5

[FILTER]
    Name         modify
    Match        *
    Add          environment production
    Add          region ap-northeast-2

[OUTPUT]
    Name            es
    Match           *
    Host            search-mydomain.us-east-1.es.amazonaws.com
    Port            443
    TLS             On
    AWS_Auth        On
    AWS_Region      ap-northeast-2
    Index           app-logs
    Type            _doc
```

### 3.2 Lambda를 이용한 S3 로그 전송

```python
import boto3
import json
from datetime import datetime
from opensearchpy import OpenSearch, RequestsHttpConnection
from requests_aws4auth import AWS4Auth

def lambda_handler(event, context):
    # OpenSearch 클라이언트 설정
    region = 'ap-northeast-2'
    service = 'es'
    credentials = boto3.Session().get_credentials()
    awsauth = AWS4Auth(
        credentials.access_key,
        credentials.secret_key,
        region,
        service,
        session_token=credentials.token
    )

    host = 'search-mydomain.ap-northeast-2.es.amazonaws.com'

    client = OpenSearch(
        hosts=[{'host': host, 'port': 443}],
        http_auth=awsauth,
        use_ssl=True,
        verify_certs=True,
        connection_class=RequestsHttpConnection
    )

    # S3에서 로그 읽기
    s3 = boto3.client('s3')

    for record in event['Records']:
        bucket = record['s3']['bucket']['name']
        key = record['s3']['object']['key']

        obj = s3.get_object(Bucket=bucket, Key=key)
        logs = obj['Body'].read().decode('utf-8')

        # OpenSearch에 인덱싱
        for line in logs.split('\n'):
            if line:
                log_entry = json.loads(line)
                log_entry['@timestamp'] = datetime.utcnow().isoformat()

                client.index(
                    index='app-logs',
                    body=log_entry
                )

    return {
        'statusCode': 200,
        'body': json.dumps('Success')
    }
```

---

<Callout>

### 🚀 Lambda 함수 템플릿

S3에서 OpenSearch로 로그를 전송하는 Lambda 함수 전체 코드와 CloudFormation 템플릿을 받으세요.

<DownloadButton
  assetId="opensearch-lambda-template"
  assetName="OpenSearch_Lambda_Template.zip"
  downloadUrl="/downloads/lambda-template.zip"
/>

</Callout>

---

## 4. 검색 쿼리 최적화

### 4.1 기본 검색 쿼리

```json
{
  "query": {
    "bool": {
      "must": [
        { "match": { "message": "error" } }
      ],
      "filter": [
        { "term": { "level": "ERROR" } },
        { "range": {
          "timestamp": {
            "gte": "now-1h"
          }
        }}
      ]
    }
  },
  "sort": [
    { "timestamp": { "order": "desc" } }
  ]
}
```

### 4.2 집계 (Aggregation) 활용

```json
{
  "size": 0,
  "query": {
    "range": {
      "timestamp": {
        "gte": "now-24h"
      }
    }
  },
  "aggs": {
    "errors_over_time": {
      "date_histogram": {
        "field": "timestamp",
        "interval": "1h"
      },
      "aggs": {
        "by_service": {
          "terms": {
            "field": "service",
            "size": 10
          }
        }
      }
    }
  }
}
```

### 4.3 성능 최적화 팁

1. **인덱스 설계**
   - 적절한 샤드 수 설정 (데이터 크기/노드 수 고려)
   - 시계열 데이터는 일별/월별 인덱스 분리
   - 불필요한 필드는 `_source`에서 제외

2. **쿼리 최적화**
   - `filter` context 적극 활용 (캐싱)
   - `match_phrase` 대신 `term` 사용 가능한지 검토
   - 불필요한 `script` 사용 최소화

3. **인덱스 라이프사이클 관리**
   - ISM (Index State Management) 정책 설정
   - 오래된 인덱스 자동 삭제 또는 UltraWarm 이동

---

## 5. 모니터링 및 알림 설정

### 5.1 OpenSearch Dashboards 시각화

```
대시보드 구성 예시:
┌─────────────────────────────────────┐
│  Error Rate (Last 24h)              │
│  📈 [선 그래프]                      │
└─────────────────────────────────────┘
┌─────────────────┬───────────────────┐
│  Top Errors     │  Service Health   │
│  📊 [파이 차트]  │  🟢🟡🔴 [상태]   │
└─────────────────┴───────────────────┘
┌─────────────────────────────────────┐
│  Recent Error Logs                  │
│  📋 [테이블]                         │
└─────────────────────────────────────┘
```

### 5.2 알림 설정

```json
{
  "name": "High Error Rate Alert",
  "type": "monitor",
  "enabled": true,
  "schedule": {
    "period": {
      "interval": 5,
      "unit": "MINUTES"
    }
  },
  "inputs": [{
    "search": {
      "indices": ["app-logs-*"],
      "query": {
        "size": 0,
        "query": {
          "bool": {
            "must": [
              { "term": { "level": "ERROR" } },
              { "range": { "timestamp": { "gte": "now-5m" } } }
            ]
          }
        }
      }
    }
  }],
  "triggers": [{
    "name": "Error threshold exceeded",
    "severity": "1",
    "condition": {
      "script": {
        "source": "ctx.results[0].hits.total.value > 100"
      }
    },
    "actions": [{
      "name": "Send Slack notification",
      "destination_id": "slack-channel",
      "message_template": {
        "source": "Error rate exceeded: {{ctx.results[0].hits.total.value}} errors in last 5 minutes"
      }
    }]
  }]
}
```

---

**💬 문의하기:**

<CtaButton ctaType="contact" variant="primary">
OpenSearch 구축 문의하기
</CtaButton>

<CtaButton ctaType="apply" variant="secondary">
무료 컨설팅 신청
</CtaButton>

---

## 6. 보안 및 접근 제어

### 6.1 VPC 내부 배포

```typescript
// CDK 예시
const domain = new opensearch.Domain(this, 'Domain', {
  version: opensearch.EngineVersion.OPENSEARCH_2_11,
  vpc,
  vpcSubnets: [{
    subnetType: ec2.SubnetType.PRIVATE_WITH_EGRESS,
  }],
  securityGroups: [securityGroup],
  zoneAwareness: {
    enabled: true,
    availabilityZoneCount: 2,
  },
  capacity: {
    masterNodes: 3,
    masterNodeInstanceType: 'r6g.large.search',
    dataNodes: 2,
    dataNodeInstanceType: 'r6g.xlarge.search',
  },
  ebs: {
    volumeSize: 100,
    volumeType: ec2.EbsDeviceVolumeType.GP3,
  },
  encryptionAtRest: {
    enabled: true,
  },
  nodeToNodeEncryption: true,
  enforceHttps: true,
});
```

### 6.2 세밀한 액세스 제어 (Fine-grained Access Control)

```json
{
  "backend_roles": ["admin"],
  "hosts": [],
  "users": ["admin"],
  "reserved": false,
  "hidden": false,
  "description": "Admin role with full access",
  "cluster_permissions": [
    "cluster_all"
  ],
  "index_permissions": [{
    "index_patterns": ["*"],
    "allowed_actions": ["indices_all"]
  }],
  "tenant_permissions": [{
    "tenant_patterns": ["global_tenant"],
    "allowed_actions": ["kibana_all_write"]
  }]
}
```

---

## 7. 비용 최적화 전략

### 7.1 인스턴스 타입 선택

| 사용 사례 | 추천 인스턴스 | 이유 |
|---------|------------|-----|
| 개발/테스트 | t3.small.search | 저렴한 비용 |
| 로그 분석 (쓰기 중심) | i3.xlarge.search | 높은 I/O 성능 |
| 검색 서비스 (읽기 중심) | r6g.xlarge.search | 높은 메모리, ARM 기반 저비용 |
| 대용량 데이터 | im4gn.xlarge.search | 대용량 스토리지 + ARM 성능 |

### 7.2 UltraWarm 활용

```python
# 30일 이상 된 인덱스를 UltraWarm으로 이동하는 ISM 정책
{
  "policy": {
    "description": "Move old indices to UltraWarm",
    "default_state": "hot",
    "states": [
      {
        "name": "hot",
        "actions": [],
        "transitions": [{
          "state_name": "warm",
          "conditions": {
            "min_index_age": "30d"
          }
        }]
      },
      {
        "name": "warm",
        "actions": [{
          "warm_migration": {}
        }],
        "transitions": [{
          "state_name": "delete",
          "conditions": {
            "min_index_age": "90d"
          }
        }]
      },
      {
        "name": "delete",
        "actions": [{
          "delete": {}
        }]
      }
    ]
  }
}
```

### 7.3 예약 인스턴스

- 1년/3년 예약 시 최대 **30-45% 비용 절감**
- 안정적인 워크로드에 적합
- 인스턴스 타입 변경 가능 (같은 패밀리 내)

---

## 8. 프로덕션 체크리스트

### 8.1 클러스터 설정

- [ ] 멀티 AZ 배포 활성화
- [ ] 전용 마스터 노드 3개 설정
- [ ] 데이터 노드 Auto Scaling 구성
- [ ] EBS 볼륨 GP3 사용 (비용 효율)
- [ ] 암호화 활성화 (전송 중/저장 중)

### 8.2 보안

- [ ] VPC 내부 배포
- [ ] Fine-grained Access Control 활성화
- [ ] IAM 역할 기반 접근 제어
- [ ] Security Group 최소 권한 설정
- [ ] CloudWatch Logs 전송 활성화

### 8.3 모니터링

- [ ] CloudWatch 알람 설정 (CPU, 메모리, 디스크)
- [ ] JVMMemoryPressure < 80% 알람
- [ ] ClusterStatus.red 알람
- [ ] 자동 스냅샷 활성화 (매일)
- [ ] OpenSearch 대시보드 알림 설정

### 8.4 성능

- [ ] 샤드 크기 10-50GB 유지
- [ ] 인덱스 템플릿 설정
- [ ] ISM 정책 적용
- [ ] 쿼리 성능 모니터링
- [ ] Slow logs 활성화

---

## 9. 트러블슈팅

### 9.1 클러스터 상태 Red

**원인:**
- 노드 장애
- 샤드 할당 실패
- 디스크 공간 부족

**해결:**

```bash
# 클러스터 상태 확인
GET _cluster/health

# 할당되지 않은 샤드 확인
GET _cat/shards?v&h=index,shard,prirep,state,unassigned.reason

# 샤드 재할당 시도
POST _cluster/reroute?retry_failed=true

# 디스크 공간 확인
GET _cat/allocation?v
```

### 9.2 느린 쿼리 최적화

```bash
# Slow log 설정
PUT /my-index/_settings
{
  "index.search.slowlog.threshold.query.warn": "10s",
  "index.search.slowlog.threshold.query.info": "5s",
  "index.search.slowlog.threshold.fetch.warn": "1s",
  "index.indexing.slowlog.threshold.index.warn": "10s"
}

# 프로파일 API로 쿼리 분석
GET /my-index/_search
{
  "profile": true,
  "query": { ... }
}
```

---

## 10. 결론

AWS OpenSearch는 강력한 검색 및 분석 플랫폼입니다. 이 가이드에서 다룬 내용을 요약하면:

✅ **아키텍처 설계**: 마스터 노드, 데이터 노드, UltraWarm을 활용한 확장 가능한 구조
✅ **로그 수집**: Fluent Bit, Lambda를 통한 다양한 수집 파이프라인
✅ **검색 최적화**: 쿼리 최적화 및 인덱스 설계 전략
✅ **모니터링**: 대시보드 및 알림 설정으로 안정적인 운영
✅ **보안**: VPC, Fine-grained Access Control로 보안 강화
✅ **비용 최적화**: UltraWarm, 예약 인스턴스로 비용 절감

---

<Callout>

### 🎯 OpenSearch 실전 워크샵

이 가이드의 모든 내용을 실습할 수 있는 핸즈온 워크샵에 참여하세요!

<DownloadButton
  assetId="opensearch-workshop-materials"
  assetName="OpenSearch_Workshop_Materials.zip"
  downloadUrl="/downloads/workshop-materials.zip"
>
워크샵 자료 다운로드
</DownloadButton>

<CtaButton ctaType="apply" variant="outline">
워크샵 신청하기
</CtaButton>

</Callout>

---

### 참고 자료

- [AWS OpenSearch Service 공식 문서](https://docs.aws.amazon.com/opensearch-service/)
- [OpenSearch 공식 문서](https://opensearch.org/docs/latest/)
- [AWS Database Blog](https://aws.amazon.com/blogs/database/)

**다음 읽을 글:**
- ElasticSearch vs OpenSearch 비교 분석
- Logstash, Fluent Bit 성능 벤치마크
- OpenSearch 대용량 트래픽 처리 사례 연구
